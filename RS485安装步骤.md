# 官方SDk存在问题,需要按照以下步骤修改并配置灵巧手

1. 从github上克隆官方SDK代码
```bash
git clone https://github.com/AgibotTech/Omnihand-2025-SDK.git
```

2. 进入 `src/implementation/c_agibot_hand_rs/c_agibot_hand_rs.cc` 文件，找到37行,加入一行代码：
```cpp
handrs485_interface_ =
      std::make_unique<UartRs485Interface>(options.uart_port, options.uart_baudrate);
  handrs485_interface_->InitDevice();
  is_init_ = handrs485_interface_->Rs485_device_ptr_.isOpen(); #这个代码
  // uint8_t check_cmd[] = {0xEE, 0xAA, 0x01, 0x00, 0x01, 0xCD, 0x55, 0x55};
```

3. 进入 `src/implementation/c_agibot_hand_rs/c_agibot_hand_rs.h` 修改21行代码:
```cpp
std::string uart_port = "/dev/ttyACM0";
```

4. 进入 `src/c_agibot_hand_base.cc` 修改42行代码:
```cpp
//   hand = std::make_unique<AgibotHandCanO10>(canfd_id);
        hand = std::make_unique<AgibotHandRsO10>();
```

5. 按照官方SDK的编译方式编译:
```bash
./build.sh -DCMAKE_BUILD_TYPE=Release \
             -DCMAKE_INSTALL_PREFIX=./build/install \
             -DBUILD_PYTHON_BINDING=ON \
             -DBUILD_CPP_EXAMPLES=OFF
```

6. 编译完成后,找到`src/Omnihand-2025-SDK/build/omnihand_2025_pkg/dist/omnihand_2025_py-0.8.0-cp310-cp310-linux_x86_64.whl`文件,安装到python环境中:
```bash
pip install ./omnihand_2025_py-0.8.0-cp310-cp310-linux_x86_64.whl
```

> 如果之前安装过这个包,需要覆盖安装,加上`--force-reinstall`参数:
```bash
pip install --force-reinstall ./omnihand_2025_py-0.8.0-cp310-cp310-linux_x86_64.whl
```

7. 安装完成后,进入python示例目录,运行示例代码:
```bash
cd python/example
python3 ./demo_set_motion.py
```