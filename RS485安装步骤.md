# 官方SDk存在问题,需要按照以下步骤修改并配置灵巧手

1. 从github上克隆官方SDK代码
```bash
git clone https://github.com/AgibotTech/Omnihand-2025-SDK.git
```

2. 进入 `src/implementation/c_agibot_hand_rs/c_agibot_hand_rs.cc` 文件，替换构造函数：
```cpp
AgibotHandRsO10::AgibotHandRsO10(const std::string& uart_port) {
  Options options;
  options.uart_port = uart_port;

  handrs485_interface_ =
      std::make_unique<UartRs485Interface>(options.uart_port, options.uart_baudrate);
  handrs485_interface_->InitDevice();
  is_init_ = handrs485_interface_->Rs485_device_ptr_.isOpen();
  // uint8_t check_cmd[] = {0xEE, 0xAA, 0x01, 0x00, 0x01, 0xCD, 0x55, 0x55};
  // handrs485_interface_->WriteDevice(check_cmd, sizeof(check_cmd)); //for debug the uart-485 connection
}
```

3. 进入 `src/implementation/c_agibot_hand_rs/c_agibot_hand_rs.h` 替换构造函数声明:
```cpp
explicit AgibotHandRsO10(const std::string& uart_port = "/dev/ttyACM0");
```

4. 进入 `src/c_agibot_hand_base.cc` 替换函数实现:
```cpp
std::unique_ptr<AgibotHandO10> AgibotHandO10::createHand(
    unsigned char device_id,
    unsigned char canfd_id,
    EHandType hand_type,
    const std::string& uart_port) {
  std::unique_ptr<AgibotHandO10> hand;

  hand = std::make_unique<AgibotHandRsO10>(uart_port);

  hand->Reset(device_id, hand_type);

  return hand;
}
```

5. 进入 `src/Omnihand-2025-SDK/src/c_agibot_hand_base.h` 替换工厂方法声明:
```cpp
  static std::unique_ptr<AgibotHandO10> createHand(
      unsigned char device_id,
      unsigned char canfd_id,
      EHandType hand_type,
      const std::string& uart_port = "/dev/ttyACM0");
```

6. 进入配置好的ros2节点 `src/hand_test/omnihand_node/include/omnihand_node/hand_node.h` 替换构造函数声明:
```cpp
  OmniHandProNode(uint8_t device_id, uint8_t canfd_id, EHandType hand_type,
                   const std::string& uart_port = "/dev/ttyACM0");
```

7. 进入 `src/hand_test/omnihand_node/src/hand_node.cpp` 替换构造函数实现:
```cpp
OmniHandProNode::OmniHandProNode(uint8_t device_id, uint8_t canfd_id, EHandType hand_type,
                                   const std::string& uart_port) : Node("omnihand_node" + std::to_string(device_id)) {
//   agibot_hand_ = std::make_shared<AgibotHandO12>(device_id, canfd_id, hand_type);
  agibot_hand_ = AgibotHandO10::createHand(device_id, canfd_id, hand_type, uart_port);
//到此结束,替换以上内容
  if (!agibot_hand_->Init()) {
    RCLCPP_ERROR(this->get_logger(), "Failed to initialize AgibotHandO10 device with ID %d", device_id);
    return;
  }
```

8. 进入 `src/hand_test/omnihand_node/src/main.cpp` 替换节点创建:
```cpp
  auto left_hand_node = std::make_shared<omnihand::OmniHandProNode>(0, 0, EHandType::eLeft, "/dev/omnihand_left");
```

9. 按照官方SDK的编译方式编译:
```bash
./build.sh -DCMAKE_BUILD_TYPE=Release \
             -DCMAKE_INSTALL_PREFIX=./build/install \
             -DBUILD_PYTHON_BINDING=ON \
             -DBUILD_CPP_EXAMPLES=OFF
```

6. 编译完成后,找到`src/Omnihand-2025-SDK/build/omnihand_2025_pkg/dist/omnihand_2025_py-0.8.0-cp310-cp310-linux_x86_64.whl`文件,安装到python环境中:
```bash
pip install ./omnihand_2025_py-0.8.0-cp310-cp310-linux_x86_64.whl
```

> 如果之前安装过这个包,需要覆盖安装,加上`--force-reinstall`参数:
```bash
pip install --force-reinstall ./omnihand_2025_py-0.8.0-cp310-cp310-linux_x86_64.whl
```

7. 安装完成后,进入python示例目录,运行示例代码:
```bash
cd python/example
python3 ./demo_set_motion.py
```