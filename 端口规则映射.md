# 端口udev规则映射步骤
> 本演示适用于虚拟机或者主机链接(usb/rs485通信)

1. 将灵巧手(左or右)插入并通电后
```ll /dev/ | grep ttyACM```

2. 查看当前灵巧手的唯一属性
```udevadm info -a -n /dev/ttyACM0 | grep -E "serial|idProduct|idVendor"```

    ATTRS{idProduct}=="5740"  # 厂商ID
    ATTRS{idVendor}=="0483"   # 产品ID
    ATTRS{serial}=="207D34844B45" # 序列号(唯一)

3. 创建udev规则文件(以左手为例)
```bash
sudo nano /etc/udev/rules.d/omnihand.rules
```
```
SUBSYSTEM=="tty", ATTRS{idVendor}=="0483", ATTRS{idProduct}=="5740", ATTRS{serial}=="207D34844B45", SYMLINK+="omnihand_left", MODE="0666"
```

> 如果是右手,重复上述过程,打开相同规则文件,复制第一行,粘贴到下一行,修改序列号为右手的唯一属性,并将`omnihand_left`改为`omnihand_right`

4. 重载规则

```bash
sudo udevadm control --reload-rules
sudo udevadm trigger
```

5. 验证是否生效
```bash
ll /dev/ | grep omnihand
```