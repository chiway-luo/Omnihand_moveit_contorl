# USB-C 虚拟串口 udev 规则映射步骤
> 当前 O10 通过 USB-C CDC 虚拟串口连接，不使用外置 RS485 转接器。节点统一访问稳定名称 `/dev/omnihand_left`，不要在代码中依赖可能变化的 `/dev/ttyACM0`。

1. 将灵巧手（左或右）插入并通电后，确认系统分配的设备节点：

```bash
ls -l /dev/ttyACM*
```

2. 查看当前灵巧手的唯一属性
```bash
udevadm info -a -n /dev/ttyACM0 | grep -E "serial|idProduct|idVendor"
```

    ATTRS{idProduct}=="5740"  # 产品 ID
    ATTRS{idVendor}=="0483"   # 厂商 ID
    ATTRS{serial}=="316837753235" # 序列号(唯一)

3. 创建udev规则文件(以左手为例)
```bash
sudo nano /etc/udev/rules.d/omnihand.rules
```
```
SUBSYSTEM=="tty", ATTRS{idVendor}=="0483", ATTRS{idProduct}=="5740", ATTRS{serial}=="316837753235", SYMLINK+="omnihand_left", MODE="0666"
```

> 如果是右手,重复上述过程,打开相同规则文件,复制第一行,粘贴到下一行,修改序列号为右手的唯一属性,并将`omnihand_left`改为`omnihand_right`

4. 重载规则

```bash
sudo udevadm control --reload-rules
sudo udevadm trigger
```

5. 验证是否生效
```bash
ls -l /dev/omnihand_left
readlink -f /dev/omnihand_left
```

当前左手应解析到 `/dev/ttyACM0`。规则中的 `MODE="0666"` 生效后，无需每次手动执行 `chmod`。
